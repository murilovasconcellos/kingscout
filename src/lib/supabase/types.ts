/**
 * Tipos do schema do banco (times, jogadores, partidas, períodos,
 * catálogos e eventos).
 *
 * Placeholder da Fase 2 (Estrutura de pastas). A partir da Fase 3 (Banco
 * de dados), este arquivo passa a conter os tipos gerados automaticamente
 * via Supabase CLI (`supabase gen types typescript`), evitando duplicar
 * manualmente a definição do schema em dois lugares.
 */
export {};
