/**
 * useEventRegister — função central que cria um evento localmente
 * (escrita otimista) e o enfileira na outbox para persistência assíncrona.
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação prevista na
 * Fase 10 (Sistema de eventos), conforme Arquitetura e Especificação
 * Funcional (regra 3.1).
 */
export function useEventRegister(): void {
  // TODO: implementar na Fase 10.
}
