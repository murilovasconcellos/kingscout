/**
 * Tipos de domínio relacionados a Jogador (Player, PlayerStatus).
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Definição adiada
 * deliberadamente para a Fase 3 (Banco de dados) — ver justificativa em
 * `match.ts`.
 */
export {};
