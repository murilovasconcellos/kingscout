/**
 * Rota: /matches
 *
 * Dashboard: listagem e criação de partidas.
 * Placeholder da Fase 2 (Estrutura de pastas) — implementação funcional
 * prevista na Fase 7 (Criação de partidas) e Fase 18 (Dashboard).
 */
export default function MatchesDashboardPage() {
  return (
    <main>
      <p>Dashboard de partidas — implementação prevista nas Fases 7 e 18.</p>
    </main>
  );
}
