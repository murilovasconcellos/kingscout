/**
 * Rota: /matches/[matchId]/summary
 *
 * Resumo pós-jogo, somente leitura.
 * Placeholder da Fase 2 (Estrutura de pastas) — implementação funcional
 * prevista na Fase 17 (Tela de resumo da partida).
 */
export default function MatchSummaryPage() {
  return (
    <main>
      <p>Resumo da partida — implementação prevista na Fase 17.</p>
    </main>
  );
}
