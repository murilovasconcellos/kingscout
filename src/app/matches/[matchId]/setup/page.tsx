/**
 * Rota: /matches/[matchId]/setup
 *
 * Seleção de elenco e titulares antes do início da partida.
 * Placeholder da Fase 2 (Estrutura de pastas) — implementação funcional
 * prevista na Fase 8 (Tela de Setup da partida).
 */
export default function MatchSetupPage() {
  return (
    <main>
      <p>Setup da partida — implementação prevista na Fase 8.</p>
    </main>
  );
}
