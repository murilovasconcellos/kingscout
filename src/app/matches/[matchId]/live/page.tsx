/**
 * Rota: /matches/[matchId]/live
 *
 * Tela principal de operação — única tela usada durante a partida.
 * Placeholder da Fase 2 (Estrutura de pastas) — implementação funcional
 * prevista na Fase 9 (estrutura da Tela Live) em diante.
 */
export default function MatchLivePage() {
  return (
    <main>
      <p>Tela Live — implementação prevista a partir da Fase 9.</p>
    </main>
  );
}
