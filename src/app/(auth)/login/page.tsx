/**
 * Rota: /login
 *
 * Autenticação do operador (e-mail/senha via Supabase Auth).
 * Placeholder da Fase 2 (Estrutura de pastas) — implementação funcional
 * prevista na Fase 4 (Autenticação).
 */
export default function LoginPage() {
  return (
    <main>
      <p>Login — implementação prevista na Fase 4.</p>
    </main>
  );
}
