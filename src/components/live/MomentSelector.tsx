/**
 * MomentSelector — barra dos 6 momentos da Kings League (badge + troca).
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação funcional
 * prevista a partir da Fase 9 (estrutura da Tela Live), com o comportamento
 * de troca de momento (fecha/abre `match_period`) descrito na Especificação
 * Funcional (regras de Partida) e no documento de UX (seção 6).
 */
export default function MomentSelector() {
  return null;
}
