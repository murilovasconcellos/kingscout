/**
 * GoalAssistPrompt — prompt não bloqueante de assistência após um gol.
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação funcional
 * prevista na Fase 10 (Sistema de eventos) / Fase 12 (Placar automático),
 * conforme UX (seção 12) e Especificação Funcional (regra 5.2).
 */
export default function GoalAssistPrompt() {
  return null;
}
