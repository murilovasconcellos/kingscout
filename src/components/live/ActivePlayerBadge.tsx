/**
 * ActivePlayerBadge — destaque do jogador atualmente selecionado.
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação funcional
 * prevista na Fase 9 (estrutura da Tela Live), conforme UX (seção 1).
 */
export default function ActivePlayerBadge() {
  return null;
}
