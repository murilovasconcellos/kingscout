/**
 * EventFeed — histórico recente de eventos, com ação de desfazer por item.
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação funcional
 * prevista na Fase 14 (Timeline), conforme UX (seção 3) e Especificação
 * Funcional (regra 3.2/3.3 — editar/excluir evento).
 */
export default function EventFeed() {
  return null;
}
