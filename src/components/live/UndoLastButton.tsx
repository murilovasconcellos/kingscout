/**
 * UndoLastButton — atalho fixo (botão flutuante) para desfazer o último evento.
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação funcional
 * prevista na Fase 15 (Undo), conforme UX (seção 8) e Especificação
 * Funcional (regra 3.4).
 */
export default function UndoLastButton() {
  return null;
}
