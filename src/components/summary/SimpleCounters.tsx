/**
 * SimpleCounters — tabela simples de contadores por jogador (somente leitura).
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação funcional
 * prevista na Fase 16 (Estatísticas automáticas) / Fase 17 (Tela de resumo).
 */
export default function SimpleCounters() {
  return null;
}
