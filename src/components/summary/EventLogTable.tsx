/**
 * EventLogTable — log cronológico de eventos da partida, agrupado por momento.
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação funcional
 * prevista na Fase 17 (Tela de resumo da partida).
 */
export default function EventLogTable() {
  return null;
}
