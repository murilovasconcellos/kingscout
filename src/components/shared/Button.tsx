/**
 * Button — botão base reutilizável em todo o sistema.
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação alinhada ao
 * Design System (seção 5.1) a partir da primeira fase que o utilizar
 * (Fase 7/8 em diante).
 */
export default function Button() {
  return null;
}
