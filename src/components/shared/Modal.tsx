/**
 * Modal — card flutuante não bloqueante (base para prompts como o de
 * assistência pós-gol e o de correção de evento na timeline).
 *
 * Placeholder da Fase 2 (Estrutura de pastas). Implementação alinhada ao
 * Design System (seção 5.2) a partir da Fase 10/14.
 */
export default function Modal() {
  return null;
}
