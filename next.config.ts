import type { NextConfig } from "next";

/**
 * Nenhuma configuração customizada é necessária nesta fase.
 * Ajustes futuros (ex: domínios de imagem remota para logos de time)
 * serão adicionados aqui conforme as fases correspondentes do roadmap
 * exigirem.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
